name: Repository Events version 1
on: 
    push:
        branches:
        - main
    pull_request:
        types: [opened, assigned, synchronize, reopened]
    issues:
        types: [opened, edited, closed]

jobs:
  checkout-test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF
      - uses: actions/checkout@v3